<%= form_with(model: fase_type, local: true) do |form| %>
  <% if fase_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fase_type.errors.count, "error") %> prohibited this fase_type from being saved:</h2>

      <ul>
      <% fase_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :fase_type_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
